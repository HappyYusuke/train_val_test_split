# train_val_test_split
本リポジトリは、データセットをTRAIN、VAL、TESTに分割します。

<br>

# Requirements
本リポジトリは以下環境で開発しました。

| 項目 | 要件 |
| --- | --- |
| Python | 3.10.12 |
| tqdm | 4.67.1 |

<br>

# Installation
`tqdm`をインストール
```bash
pip install tqdm
```

<br>

# Usage
**1. データセットを配置** <br>
データセットを以下のように配置してください。

```bash
your_dataset 👉 親ディレクトリ
├── images   👉 学習させるファイルが入ったディレクトリ
└── labels   👉 ラベルが入ったディレクトリ
```

**2. 分割** <br>
`random_split.py`を使用します。<br>
引数は以下の通りです。

| 引数 | 初期値 | 内容 |
| --- | --- | --- |
| `-i`or`--images` | - | 学習データまでのパス |
| `-l`or`--labels` | - | ラベルデータまでのパス |
| `-s`or`--savename` | `split_result` | 保存するディレクトリ名 (パスを指定することも可能です) |
| `-r`or`--ratio` | `16 4 5` | `TRAIN`, `VAL`, `TEST`の比率 |

引数を指定して`random_split.py`を実行。
```bash
python3 random_split.py -i /path/to/your_data/images -l /path/to/your_data/labels
```

`TRAIN:VAL:TEST`の比率を`8:1:1`で指定する場合。
```bash
python3 random_split.py -i /path/to/your_data/images -l /path/to/your_data/labels -r 8 1 1
```
